deploy:
  stage: deploy
  image: ghcr.io/railwayapp/cli:latest
  variables:
    # SVC_ID can also be set as a CI/CD variable if you prefer
    SVC_ID: subscription_bot

  script:
    - railway up --service="$SVC_ID"